// Este arquivo não é mais usado.
// A aplicação agora usa React Router configurado em main.tsx
// Mantido apenas para evitar erros de importação legado

export default function App() {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <p className="text-gray-600">
        Redirecionando... Se você ver esta mensagem, verifique o main.tsx
      </p>
    </div>
  );
}
